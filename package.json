{
  "name": "drizzle",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch .",
    "db:generate": "bun drizzle-kit generate",
    "db:push": "bun drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.23.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@libsql/client": "^0.8.1",
    "drizzle-orm": "^0.32.2"
  }
}
